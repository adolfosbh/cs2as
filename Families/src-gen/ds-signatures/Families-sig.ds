module src-gen/ds-signatures/Families-sig

imports src-gen/ds-signatures/Common-sig

signature
    sorts
      Start
      ChildrenCS
    constructors
      FamilyCS : ID * ID * ID * List(ChildrenCS) -> Start 
      ChildrenCS : ID -> ChildrenCS 